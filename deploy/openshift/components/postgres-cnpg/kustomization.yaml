apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# Kustomize-component: voegt het CloudNativePG-cluster toe aan een overlay
# (`components: [../../components/postgres-cnpg]`). Zo deelt de beproeving/managed-prod één definitie.
resources:
  - cluster.yaml
